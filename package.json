{
  "name": "inweb-onepager",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch --import remix/node-tsx server.ts",
    "start": "node --env-file-if-exists=.env --import remix/node-tsx server.ts",
    "test": "node --env-file-if-exists=.env --import remix/node-tsx --test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24.3.0"
  },
  "dependencies": {
    "remix": "^3.0.0-beta.2"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
